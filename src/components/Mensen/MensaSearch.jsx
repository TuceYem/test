/*import {useDefaultHomeFetch} from "../../assets/utils/API";
import MensaCard from "./MensaCard";

function MensaSearch() {
    const { canteens } = useDefaultHomeFetch()

    return (
        <>
            <div className="mensa-header">
                These are the canteens <span className="special-title text-uppercase">closest</span> to you
            </div>
            {canteens.map((mensa) => (
                <MensaCard mensa={mensa} />
            ))}
        </>
    )
}

export default MensaSearch
*/